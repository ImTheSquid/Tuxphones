{
  "name": "tuxphones",
  "version": "0.1.0",
  "description": "Discord soundshare for Linux",
  "main": "index.jsx",
  "scripts": {
    "build": "esbuild src/Tuxphones/index.jsx --bundle --outfile=src/Tuxphones/bundled.js --platform=node && zpl build Tuxphones",
    "init": "zpl init"
  },
  "author": "Jack Hogan",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.15.16",
    "zerespluginlibrary": "^2.0.6"
  },
  "zplConfig": {
    "base": "./src",
    "out": "./release",
    "copyToBD": true,
    "addInstallScript": true
  }
}
